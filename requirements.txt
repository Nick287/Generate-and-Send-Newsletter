# Core pipeline dependencies
feedparser==6.0.12
trafilatura==2.0.0
readability-lxml==0.8.4.1
requests==2.33.1
pyyaml==6.0.3
lxml==6.1.0
python-dotenv>=1.0.0
pydantic>=2.0
agent-framework>=1.2.0
azure-ai-agentserver-agentframework>=1.0.0b16

# Observability (optional — only needed for local Visualizer / DevUI tracing)
# opentelemetry-exporter-otlp-proto-grpc

# DevUI (optional — browser-based workflow debugger)
# agent-framework-devui

# Email providers (install only what you need)
azure-communication-email==1.1.0
# sendgrid==6.11.0    # uncomment if using SendGrid

# Optional: for Nick's original newsletter_git_action.py path
openai
markdown
html2text
